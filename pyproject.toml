[project]
name = "embrace-readonly-full-data"
version = "0.1.0"
description = "Full-fidelity read-only proxy for the published Embrace MCP tools"
authors = [{ name = "kokorolx" }]
requires-python = ">=3.10"
dependencies = [
  "mcp>=1.0,<2",
]

[tool.uv]
package = false
